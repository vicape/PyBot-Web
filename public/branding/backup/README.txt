Backups del logo PyBot

EN USO (mascota robot - vectorial SVG):
  Header: ../pybot-mark-robot.svg (robot) + ../pybot-wordmark.svg (PyBot) + "// by VIC" (CSS)
  Acerca de: ../pybot-logo-full.svg (lockup completo robot + PyBot)
  Favicon y marca de agua del editor: ../pybot-mark-robot.svg
  Vectorial: escala perfecto, sin pixelado ni halos.

  Respaldos: pybot-logo.mascota-svg.svg / pybot-mark.mascota-robot-svg.svg
             pybot-wordmark.mascota-svg.svg
             pybot-logo.mascota-source.png (PNG original que mandaste)

VERSION CHIP (anterior, tambien vectorial):
  pybot-mark.chip-svg.svg    - icono chip <PyBot/> (por si se quiere volver)

PARA VOLVER ATRAS:
  El logo actual es vectorial (SVG) + texto CSS, no depende de los PNG.
  La forma mas simple de revertir es con git (git checkout de
  src/PyBotIDE.jsx, src/PyBotIDE.css, index.html y public/branding).

  Los PNG de versiones previas siguen aca por si se necesitan:
    .\scripts\restore-logo.ps1 before-chip   # robot cartoon (solo copia PNG)
  (ojo: el app hoy usa el SVG; para volver al PNG hay que reeditar el JSX/CSS).

OTROS ARCHIVOS:
  pybot-logo.before-chip.png / pybot-mark.before-chip.png  - snapshot pre-cambio
  pybot-logo.chip-dark.png / pybot-mark.chip.png           - chip procesado
  pybot-logo.chip-*-source.png                             - fuentes originales
  pybot-logo.v5-clean.png / pybot-mark.v5.png              - robot limpio v5
