/** Ejemplos alineados al estilo PyBot de escritorio (sin async/await). */

export const DEFAULT_CODE = [
  "# Mismo estilo que PyBot de escritorio (sin async/await)",
  "def main():",
  '    print("PyBot Web — conectá USB y ejecutá")',
  "    motor(10, 50)",
  "    wait(1)",
  "    motor(10, 0)",
  "",
  "main()",
  "",
].join("\n");

export const EXAMPLES = [
  {
    id: "blink",
    keyEs: "Blink",
    keyEn: "Blink",
    file: "blink.py",
    code: [
      "def main():",
      "    while True:",
      '        pin("out", 2, 1)',
      "        wait(0.5)",
      '        pin("out", 2, 0)',
      "        wait(0.5)",
      "",
      "main()",
      "",
    ].join("\n"),
  },
  {
    id: "motor",
    keyEs: "Motor",
    keyEn: "Motor",
    file: "motor.py",
    code: [
      "def main():",
      "    motor(10, 80)",
      "    wait(2)",
      "    motor(10, -60)",
      "    wait(2)",
      "    motor(10, 0)",
      "",
      "main()",
      "",
    ].join("\n"),
  },
  {
    id: "servo",
    keyEs: "Servo",
    keyEn: "Servo",
    file: "servo.py",
    code: [
      "def main():",
      "    servo(10, 0)",
      "    wait(1)",
      "    servo(10, 90)",
      "    wait(1)",
      "    servo(10, 180)",
      "",
      "main()",
      "",
    ].join("\n"),
  },
  {
    id: "light_sensor",
    keyEs: "Sensor de luz",
    keyEn: "Light sensor",
    file: "light_sensor.py",
    code: [
      "def main():",
      "    for _ in range(20):",
      '        luz = pin("in", "A0")',
      "        pct = int(luz / 1023 * 100)",
      '        print(f"Luz: {pct}%")',
      "        wait(0.3)",
      "",
      "main()",
      "",
    ].join("\n"),
  },
  {
    id: "button",
    keyEs: "Pulsador",
    keyEn: "Button",
    file: "button.py",
    code: [
      "def main():",
      "    while True:",
      '        if pin("in", 7):',
      '            pin("out", 2, 1)',
      "        else:",
      '            pin("out", 2, 0)',
      "        wait(0.05)",
      "",
      "main()",
      "",
    ].join("\n"),
  },
];
